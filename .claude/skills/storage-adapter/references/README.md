# Storage Adapter — References

Map of files in this directory. Read only what the current task needs.

| File | Read when |
|------|-----------|
| `contract.md` | Implementing or consuming `StorageAdapter`; need the interface, key naming, upload validation |
| `local-adapter.md` | Working with the local FS backend; permissions, Range requests, path traversal |
| `s3-adapter.md` | Implementing or tuning the S3/R2 backend; signed URLs, bucket policy |
| `migration.md` | Planning or executing the local → S3 cutover |

{
  "name": "voicelab",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:api": "bun --cwd apps/api --env-file=../../.env --hot src/index.ts",
    "dev:web": "bun --cwd apps/web dev",
    "dev": "bun scripts/dev.ts",
    "lint": "echo \"lint not configured yet\" && exit 0",
    "typecheck": "bun --cwd apps/api typecheck && bun --cwd packages/db typecheck && bun --cwd packages/shared typecheck && bun --cwd apps/web typecheck",
    "test": "bun test",
    "db:generate": "drizzle-kit generate --config=packages/db/drizzle.config.ts",
    "db:migrate": "drizzle-kit migrate --config=packages/db/drizzle.config.ts",
    "db:seed": "bun --env-file=.env packages/db/src/seed.ts",
    "prepare": "simple-git-hooks"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "simple-git-hooks": "^2.11.1",
    "typescript": "^5.7.2"
  },
  "simple-git-hooks": {
    "pre-push": "bun run test && bun run lint && bun run typecheck"
  },
  "engines": {
    "bun": ">=1.3.13"
  }
}
